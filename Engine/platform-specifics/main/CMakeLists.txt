if (WIN32)
add_library(${PLATFORM_SPECIFICS}_main main_desktop.c)
target_link_libraries(${PLATFORM_SPECIFICS}_main ${PLATFORM_SPECIFICS}_graphics)
elseif(CMAKE_SYSTEM_NAME STREQUAL "NintendoDS")
message("The ds is the target")
add_library(${PLATFORM_SPECIFICS}_main main_ds.c)
target_link_libraries(${PLATFORM_SPECIFICS}_main ${PLATFORM_SPECIFICS}_graphics)
target_include_directories(${PLATFORM_SPECIFICS}_main PRIVATE ${BLOCKSDS_INCLUDES})
endif()
target_link_libraries(${PLATFORM_SPECIFICS} INTERFACE ${PLATFORM_SPECIFICS}_main )
