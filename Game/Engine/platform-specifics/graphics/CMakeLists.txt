if (DEFINED USE_GL)
add_compile_definitions(USE_GL)
add_subdirectory(GL)
endif()
if(DEFINED USE_GLFW)
add_compile_definitions(USE_GLFW)
endif()
if(DEFINED USE_GLEW)
add_compile_definitions(USE_GLEW)
endif()
set(LINK_TARGETS ${LINK_TARGETS} PARENT_SCOPE)